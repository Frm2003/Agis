package br.fateczl.com.agis.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.ModelMap;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;

import br.fateczl.com.agis.model.Curso;
import br.fateczl.com.agis.service.CursoService;
import jakarta.validation.Valid;

@Controller
public class CrudTurmaController {
	@Autowired
	private CursoService cserv;
	
	@GetMapping("/secretaria/turma")
	public String get(ModelMap model) {
		model.addAttribute("turma", new Curso());
		return "crudCurso/cursoForm";
	}
	
	@GetMapping("/secretaria/turma/lista")
	public String listar(ModelMap model) throws Exception {
		model.addAttribute("cursos", cserv.listarTudo());
		return "crudCurso/cursoTable";
	}
	
	@GetMapping("/secretaria/turma/buscar/{cod}")
	public String buscar(@PathVariable("cod") Long cod, ModelMap model) throws Exception {
		model.addAttribute("turma", cserv.buscar(cod));
		return "crudCurso/cursoEdit";
	}
	
	@GetMapping("/secretaria/turma/deletar/{cod}")
	public String deletar(@PathVariable("cod") Long cod) throws Exception {
		cserv.remover(cod);
		return "redirect:/curso/lista";
	}
	
	@PostMapping("/secretaria/turma/inserir")
	public String Inserir(@Valid Curso c) {
		cserv.inserir(c);
		return "redirect:/turma/lista";
	}
	
	@PostMapping("/secretaria/turma/atualizar/{cod}")
	public String atualizar(@Valid Curso c) throws Exception {
		cserv.atualizar(c);
		return "redirect:/turma/lista";
	}
}
