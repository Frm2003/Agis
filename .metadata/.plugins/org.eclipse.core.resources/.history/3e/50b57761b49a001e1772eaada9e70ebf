<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">

<head>
	<meta charset='utf-8'>
	<title>Page Title</title>
	<meta name='viewport' content='width=device-width, initial-scale=1'>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
		integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
	<link rel='stylesheet' type='text/css' media='screen' href='main.css'>
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
		integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
		crossorigin="anonymous"></script>
	<script src='main.js'></script>
	<style>
		body {
			background-color: #343a40;
			/* Cor de fundo escura */
			color: #ffffff;
			/* Cor do texto */
		}

		.container {
			background-color: #454d55;
			/* Cor de fundo do container */
			padding: 20px;
			border-radius: 8px;
			/* Bordas arredondadas para o container */
			margin-top: 50px;
			/* Margem superior para o container */
		}

		.btn-primary {
			background-color: #007bff;
			/* Cor de fundo do botão primário */
			border-color: #007bff;
			/* Cor da borda do botão primário */
		}

		.btn-primary:hover {
			background-color: #0056b3;
			/* Cor de fundo do botão primário ao passar o mouse */
			border-color: #0056b3;
			/* Cor da borda do botão primário ao passar o mouse */
		}

		form {
			border-bottom: 1px solid rgba(255, 255, 255, 0.5);
			padding-bottom: 0.5em;
		}

		form h3 {
			padding: 0.5em 0em 0.5em 0em;
		}
	</style>
</head>

<body>
	<nav>
		<div th:replace="~{nav :: nav}"></div>
	</nav>
	<div class="container">
		<div class="row justify-content-center">
			<div class="col-md-9">

				<form role="form" th:action="@{/secretaria/grade/montar}" method="post">
					<h3>Selecione os curso</h3>
					<div class="d-flex justify-content-evenly">
						<select class="form-control" name="codCurso">
							<option th:each="curso : ${cursos}" th:value="${curso.cod}">
								<span th:text="${curso.nome}"></span> - <span th:text="${curso.turno}"></span>
							</option>
						</select>
						<button type="submit" class="btn btn-primary">Buscar</button>
					</div>
				</form>

				<div th:if="${turmas != null}">
					<form role="form" th:action="@{/secretaria/montarGrade/addTurma}" method="post">
						<h3>Dados da grade</h3>
						<div class="d-flex justify-content-evenly" th:object="${curso}">
							<input type="hidden" name="codCurso" th:value="${curso.cod}">
							<input type="hidden" name="ano" th:value="${ano}">
							<input type="hidden" name="semestre" th:value="${semestre}">

							<p th:text="${nomeCurso}"></p>
							<p th:text="${ano}"></p>
							<p th:text="${semestre}"></p>
							<button type="submit" class="btn btn-primary"><i class="fa-solid fa-plus"></i></button>
						</div>
					</form>
				</div>

				<form role="form" th:action="@{/secretaria/montarGrade/addGrade}" method="post">
					<div th:object="${curso}">
						<input type="hidden" name="codCurso" th:value="${curso.cod}">
						<input type="hidden" name="ano" th:value="${ano}">
						<input type="hidden" name="semestre" th:value="${semestre}">
					</div>
					<div th:if="${turmas != null}">
						<table class="table table-dark">
							<thead>
								<tr>
									<th scope="col">Cod</th>
									<th scope="col">Horário de Início</th>
									<th scope="col">Horário de Fim</th>
									<th scope="col">Dia da Semana</th>
									<th scope="col">Disciplina</th>
									<th scope="col">Professor</th>
								</tr>
							</thead>
							<tbody>
								<tr th:each="turma : ${turmas}">
									<input type="hidden" name="codTurma" th:value="${turma.cod}">
									<td th:text="${turma.cod}"></td>
									<td>
										<select class="form-control" name="horarioInicio">
											<option>14:50</option>
											<option>13:00</option>
											<option>16:20</option>
										</select>
									</td>
									<td>
										<select class="form-control" name="horarioFim">
											<option>14:50</option>
											<option>18:20</option>
										</select>
									</td>
									<td>
										<select class="form-control" name="diaDaSemana">
											<option>Segunda</option>
											<option>Terça</option>
											<option>Quarta</option>
											<option>Quinta</option>
											<option>Sexta</option>
											<option>Sábado</option>
										</select>
									</td>
									<td>
										<select class="form-control" name="codDisciplina">
											<option th:each="d : ${disciplinas}" th:value="${d.cod}">
												<span th:text="${d.nome}"></span>
											</option>
										</select>
									</td>
									<td>
										<select class="form-control" name="codProfessor">
											<option th:each="p : ${professores}" th:value="${p.cod}">
												<span th:text="${p.nome}"></span>
											</option>
										</select>
									</td>
								</tr>
							</tbody>
						</table>
						<button type="submit">Teste</button>
					</div>

				</form>

			</div>
		</div>
	</div>
</body>

</html>