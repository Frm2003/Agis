<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tabela de Professores</title>
    <!-- Adicione links para Bootstrap ou outros estilos, se necessário -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="d-flex justify-content-center">
                <h2>Tabela de Professores</h2>
            </div>
            <table class="table table-dark">
                <thead>
                    <tr>
                        <th scope="col">Cod</th>
                        <th scope="col">Nome</th>
                        <th scope="col">Titulação</th>
                        <th scope="col">Email Pessoal</th>
                        <th scope="col">Email Corporativo</th>
                        <th scope="col" colspan="2">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <tr th:each="professor : ${professores}">
                        <td th:text="${professor.cod}"></td>
                        <td th:text="${professor.nome}"></td>
                        <td th:text="${professor.titulacao}"></td>
                        <td th:text="${professor.emailPessoal}"></td>
                        <td th:text="${professor.emailCorp}"></td>
                        <td>
                            <a th:href="@{'/professor/editar/'+${professor.cod}}">Editar</a>
                        </td>
                        <td>
                            <a th:href="@{'/professor/remover/'+${professor.cod}}">Remover</a>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

</body>
</html>
